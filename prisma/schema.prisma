generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model signups_daily {
  id    DateTime @id @db.Timestamptz(6)
  value Int?
}

model tickets_daily {
  id    DateTime @id @db.Timestamptz(6)
  value Int?
}
